<!DOCTYPE html>
<html>
  <head>
    <title>Admin - Yae Publishing House</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header style="background: #5b7c99; color: white; padding: 1rem;">
      <div style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center;">
        <h1 style="margin: 0;">Yae Publishing House - Admin</h1>
        <% if session[:admin_id] %>
          <%= button_to "Logout", admin_logout_path, method: :delete, style: "color: white; text-decoration: none; background: #e74c3c; padding: 0.5rem 1rem; border-radius: 4px; border: none; cursor: pointer;" %>
        <% end %>
      </div>
    </header>

    <% if flash[:notice] %>
      <div style="background: #d4edda; color: #155724; padding: 1rem; margin: 1rem auto; max-width: 1200px; border-radius: 4px;">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div style="background: #f8d7da; color: #721c24; padding: 1rem; margin: 1rem auto; max-width: 1200px; border-radius: 4px;">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <main style="max-width: 1200px; margin: 2rem auto; padding: 0 1rem;">
      <%= yield %>
    </main>
  </body>
</html>